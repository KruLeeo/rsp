# 1. Базовый образ
FROM python:3.9-slim

# 2. Установка рабочей директории
WORKDIR /app

# 3. Копирование файла зависимостей и их установка
COPY requirements.txt .
RUN pip install -r requirements.txt

# 4. Копирование кода приложения
COPY . .

# 5. Указание порта, который будет открывать контейнер
EXPOSE 5000

# 6. Команда запуска приложения
CMD ["python", "lab5.py", "--host=0.0.0.0", "--port=5000"]
# (Замените 'app.py' на имя вашего главного файла, например, 'manage.py' или 'server.js')